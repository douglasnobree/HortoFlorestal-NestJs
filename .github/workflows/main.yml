name: Deploy

on:
  push:
    branches: [master]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v1
      
      - name: Setup Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 20.x
          # Especifique o diretório de instalação do Node.js
          node-path: './node_modules'

      - name: NPM Install
        run: npm install

      - name: Setup Node.js
        run: |
          curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh | bash
          export NVM_DIR="$HOME/.nvm"
          [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
          [ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"
          nvm install 20
          which npm

      - name: Deploy using ssh
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 22
          script: |
            eval "$(ssh-agent -s)"
            ssh-add ~/.ssh/hortoflorestal
            ls -a
            cd appnest
            git pull
            /home/runner/.nvm/versions/node/v20.12.1/bin/npm i
            /home/runner/.nvm/versions/node/v20.12.1/bin/npm run build
            /home/runner/.nvm/versions/node/v20.12.1/bin/npm run start:prod
